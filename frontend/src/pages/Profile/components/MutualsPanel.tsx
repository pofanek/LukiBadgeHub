import { useState } from "react";
import { userchomik } from "../../../assets";
const people = ["Cwel", "Lucas |14", "Rotrix", "Slejina", "Kira"];
const categories = ["Mutuals", "Following", "Followers"] as const;
function MutualsPanel() { const [activeCategory, setActiveCategory] = useState<(typeof categories)[number]>("Mutuals"); return <div className="mx-auto max-w-2xl"><div className="border-border mb-4 flex justify-center border-b">{categories.map((category) => <button key={category} onClick={() => setActiveCategory(category)} className={`relative px-4 py-3 text-sm ${activeCategory === category ? "text-font-primary" : "text-font-muted"}`}>{category}{activeCategory === category && <span className="bg-accent-cold absolute right-3 bottom-0 left-3 h-0.5" />}</button>)}</div><div className="border-border bg-surface/75 divide-border overflow-hidden rounded-xl border divide-y">{people.map((name, index) => <div key={name} className="flex items-center gap-3 p-3 sm:p-4"><img src={userchomik} alt="" className="bg-surface-raised h-10 w-10 rounded-lg object-cover" /><div className="min-w-0 flex-1"><p className="text-font-primary truncate font-medium">{name}</p><p className="text-font-muted truncate text-xs">{activeCategory === "Followers" ? "Follows you" : `Following since ${["Oct 30, 2025", "Aug 29, 2025", "Aug 29, 2025", "Jul 6, 2025", "May 18, 2025"][index]}`}</p></div><button className="border-border bg-brand-secondary text-font-primary hover:bg-brand-primary rounded-md border px-2.5 py-1.5 text-xs transition-colors">Following</button></div>)}</div></div>; }
export default MutualsPanel;
