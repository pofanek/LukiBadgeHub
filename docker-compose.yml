services:
    frontend:
        build: ./frontend
        env_file:
            - .env.owner
        ports:
            - "5173:5173"
        volumes:
            - ./frontend:/app
            - /app/node_modules
